# AIASF n8n 워크플로우 연동

## ✅ 완료된 작업 (2025-12-23)
- [x] TTS 외계어 문제 해결 - n8n Expression 모드 적용
- [x] Code 노드 v2.0 업데이트 (TTS 분리, 자막 10개, 폰트 46px)
- [x] BGM 추가 (볼륨 0.3)

## ⚠️ 미해결 이슈
- [ ] 자막 양옆 간격 깨짐 (Shotstack HTML 렌더링 이슈)
- [ ] 영상제목 자동화 (YouTube 업로드 시)
- [ ] 인트로/아웃트로 분리 (현재 동일 영상 사용 중)

## 🔴 핵심 교훈
> **n8n JSON 필드에서 동적 값 사용 시 반드시 Expression 모드 사용!**
> `"Fixed"` 모드에서 `{{ expression }}`은 문자열 그대로 전송됨
> `={{ JSON.stringify({...}) }}` 형태로 전체를 expression으로 감싸야 함
